문서명: 온보딩 온톨로지 기반 정보 응답 시스템 – 프로젝트 문서
버전: v0.1 (초안)
작성일: 2025-10-26
작성/관리: 하이페리얼 테크놀로지스 BTS 팀

### 1. 배경 및 목표
온보딩 에이전트는 다음 입력(기본정보 입력 설문양식, 학습스타일 입력 양식, 신규상담 녹취)을 기반으로 KTM(수학학원)에 등록한 학생의 시작 정보를 체계화하고, 이후 다른 에이전트들이 필요로 하는 정보를 신뢰도 있게 제공한다. 정보 요청에 대해 어떤 정보를 어떤 가중치로 제공할지 결정하는 과정은 복잡하므로, 용어집과 온톨로지를 구축하고 해당 온톨로지 시스템이 요청을 해석·추론하여 결과를 생성, GPT API에 전달하는 흐름을 마련한다.

본 문서의 목표:
- 온보딩 도메인 용어집과 온톨로지의 초안을 정의
- 데이터 수집–정제–추론–응답 아키텍처 및 파이프라인 정의
- 정보 가중치(신뢰도) 모델과 근거 추적(출처/시간/정확도) 체계 수립
- 외부(다른 에이전트, GPT) 연동 API 규격과 프롬프트 포맷 정의
- 평가/거버넌스/보안 원칙과 운영 로드맵 제시

### 2. 범위와 비범위
- 범위: 온보딩 입력(기본정보 설문, 학습스타일 입력, 신규 상담 녹취) 처리, 온톨로지/추론/가중치 기반 정보 제공, GPT 연동을 통한 응답 생성.
- 비범위: 장기 시계열 학습 성과 예측 모델링, 고급 감정/행동 실시간 트래킹, 멀티모달(영상) 처리는 후속 버전에서 다룸.

### 3. 입력 데이터 정의
- 기본정보 입력 설문양식: 학생 인적·연락·학교·학년·선호과목·현재 성취도·단기/중기 목표·제약사항(시간/비용/개별지도 여부) 등.
- 학습스타일 입력 양식(‘학습스타일 엽력 양식’으로도 지칭됨): 선호 학습 방식(시각/청각/실기), 집중 패턴, 과제 선호, 메타인지 수준(자가 점검 습관), 협업/개별 선호 등.
- 신규상담 녹취: 상담 음성 → STT 전사 → 화자 분리(SDR), 구간 단위 요약, 엔티티/관계 추출, 애매 근거 표기(불확실성 태깅).

### 4. 용어집(Glossary) 및 온톨로지 범주
- 핵심 엔티티: Student, Guardian, Institution(KTM), IntakeSurvey, LearningStyleProfile, CounselingSession, Goal(단기/중기), Subject(수학), Topic, Concept, Misconception, Schedule, Constraint, Motivation, EmotionState, Behavior, Intervention, Recommendation, Evidence(출처/근거), Consent.
- 대표 속성 예시:
  - Student: studentId, name, school, grade, contact, consentStatus
  - LearningStyleProfile: modalityPreference, focusWindow, selfMonitoringLevel
  - CounselingSession: sessionId, dateTime, transcriptRef, summary, analystNotes
  - Evidence: sourceType(설문/녹취/교사기록), timestamp, reliabilityScore, annotator
- 대표 관계 예시:
  - Student —hasProfile→ LearningStyleProfile
  - CounselingSession —mentions→ Concept/Misconception/Constraint/Motivation
  - Evidence —supports→ Fact(임의 사실 노드)

권장 저장 모델: 운영은 Property Graph(예: Neo4j), 상호운용은 RDF/OWL 내보내기. 노드 중심 질의 성능과 스키마 진화 용이성을 확보하면서, 외부 표준과의 연계를 유지.

### 5. 가중치/신뢰도 모델
정보 제공 시 최종 가중치(가시성/우선순위)를 다음 요소의 곱으로 산정:
\( w_{final} = w_{base} \times r_{source} \times d_{recency} \times s_{specificity} \times u_{uncertainty} \)
- w_base: 사실 유형별 기본 중요도(예: 긴급 제약 > 일반 선호)
- r_source: 출처 신뢰도(검증된 설문/교사기록 > 자동 전사 요약)
- d_recency: 최신성 감쇠(최근일수록 1에 근접, 예: exp(-t/τ))
- s_specificity: 구체성(개별 개념/주제 레벨일수록 가산)
- u_uncertainty: 불확실성 보정(전사 품질/모델 확률 기반, [0,1])

초기 출처 가중치 제안(예시):
- IntakeSurvey(완결 제출): 0.85
- LearningStyleProfile(검증된 템플릿): 0.9
- CounselingTranscript(자동 전사 원문): 0.6, Summary(사람 검수): 0.8
- TeacherRecord/InstitutionalDB(검증됨): 0.95

충돌 해결 원칙:
- 동일 사실 충돌 시 더 높은 r_source, 더 높은 s_specificity, 더 최근(d_recency) 순으로 우선.
- 불일치가 지속될 경우 양자 병기 + 근거 노출, 최종판단은 다운스트림 에이전트/사람 검수 규칙에 위임 가능.

### 6. 파이프라인(ETL/E)
1) 수집: 설문 제출 Webhook, 녹취 업로드(또는 스트림), 내부 기록 동기화
2) 전처리: STT→문단/화자 분리, PII 마스킹, 품질 스코어링
3) 정보추출: 엔티티/관계/속성 추출, 불확실성 태깅, 근거(Evidence) 연결
4) 정합성 검사: 스키마 제약 검사, 값 범위/타입 검증, 충돌 탐지
5) 지속화: 그래프 DB 저장, 버전·이력(temporal) 기록, 인덱싱
6) 색인: 키워드 인덱스, 벡터 인덱스(요약/개념 임베딩)
7) 응답: 질의 → 후보 사실 검색 → 가중치 산정 → 근거 포함 요약 → GPT 생성 보조

### 7. 질의·응답 흐름 및 API
- 질의 원천: 다른 BTS 에이전트(예: Agent04 문제활동식별), 운영 콘솔, 배치 리포트
- 응답 방식: 구조적 페이로드(JSON) + natural language 요약(선택), 모든 항목에 근거/출처/시간 포함

API 초안(예시):
- POST /onboarding/ingest
  - body: { sourceType, payload, metadata }
  - resp: { status, acceptedAt, ids }
- POST /onboarding/query
  - body: { studentId, need: ["learning_style","constraints","goals"], topK: 10, timeRange?, explain: true }
  - resp: { items: [{ fact, weight, evidence:[{sourceType, timestamp, link}], explanation }], traceId }

### 8. 프롬프트 템플릿(대요)
- System:
  - 역할: 온보딩 지식 브로커. 제공 사실만 사용. 가정 금지.
  - 출력: 요약→근거 인용→권고(필요 시) 순서, 한국어.
- User에 제공 컨텍스트:
  - 학생 메타: {grade, goals, constraints}
  - 우선 사실 top-K: {fact, weight, evidence}
- Guardrails: 금칙어/PII 유출 방지, 확률적 표현 시 불확실성 명시.

### 9. 평가 및 품질 보증
- 오프라인: 골든셋(수동 라벨) 기반 정밀도/재현율, 근거 일치율, 순위 NDCG
- 온라인: 상담/추천 적합성 CSAT, 에이전트 다운스트림 성공률, 응답 시간 p95
- 회귀 테스트: 스키마 변경 시 리플레이, 질의 카나리 비교

### 10. 보안/프라이버시/거버넌스
- PII 최소 수집·암호화 저장, 전송 암호화, 접근 제어(RBAC/ABAC)
- 동의(Consent) 모델링: 사용 범위/기간/철회 반영, 감사 로그
- 데이터 보존·삭제 정책, 모델 프롬프트 로그의 민감정보 마스킹
- 변경 관리: 온톨로지 버전, 마이그레이션 스크립트, 스키마 리뷰 보드

### 11. 운영 및 버전 관리
- 스키마 버전: semver, 예: onboarding-ontology@1.0.0
- 데이터 이력: 유효기간(validFrom/To), 사실 supersede 관계
- 릴리즈 프로세스: 설계 리뷰→테스트 환경→카나리→전체 반영

### 12. 시스템 아키텍처(개요)
- 수집 계층: Web/API Gateway, File Intake(녹취), Queue(Kafka/SQS)
- 처리 계층: STT/분리, IE(NER/RE), 정합성/스코어링, 그래프 Writer
- 저장: Graph DB(Neo4j/Neptune), Object Store(STT 원본), Vector Index
- 서비스: Query Service(가중치·순위), Evidence Service, GPT Orchestrator
- 관측성: 로그/메트릭/트레이싱, 데이터 품질 대시보드

### 13. 초기 데이터 모델 예시(요약)
Fact(노드) = { type, subject, predicate, object, qualifiers, createdAt }
Evidence(노드) = { sourceType, timestamp, reliabilityScore, uri }
REL: (Evidence)-[SUPPORTS {confidence}]->(Fact), (Student)-[HAS_PROFILE]->(LearningStyle)

### 14. 로드맵(안)
- MVP(4주): 스키마 v0, 설문/녹취 수집→IE→그래프 저장, 기본 질의/가중치, GPT 연동 템플릿 v0
- v1(8주): 근거 가중치 고도화, 충돌 해소 전략, 평가 대시보드, 보안 점검
- v2(12주): 교사기록 연동, 다중 에이전트 커버리지 확장, 지식 증류/요약 자동화

### 15. 리스크 및 대응
- STT/IE 오류 → 사람 검수 워크플로, 불확실성 반영, 재학습 루프
- 스키마 폭발 → 핵심 엔티티 최소화·확장 필드로 흡수, 버전 정책 엄격 적용
- PII 리스크 → 암호화/마스킹 기본값, 접근 로깅·정기 점검

### 16. 다른 에이전트 연동 가이드(요약)
- 요청 시: need 목록과 맥락 제공(예: 목표/제약/기간)
- 응답 해석: weight 우선순위, evidence를 UI에 함께 노출 권장
- 캐싱: traceId 기반 재현 가능성 유지, TTL 정책 준수

### 17. 부록 A: 입력 스키마 예시(JSON)
IntakeSurvey(v0): { studentId, submittedAt, answers:[{key, value, confidence?}] }
LearningStyle(v0): { studentId, profile:{ modality, focusWindow, selfMonitoring }, collectedAt }
CounselingTranscript(v0): { sessionId, studentId, audioUri, stt:{ lang, quality }, segments:[{speaker, text, ts}] }

### 18. 상충 가능성 체크리스트
- 용어: ‘학습스타일 입력/엽력’ 표기 불일치가 있는지 확인 필요
- 가중치 정책: 기존 문서 내 우선순위 규정과 충돌 여부
- 데이터 보존/동의: 현행 프라이버시 정책과 기간·범위 불일치 여부
- 저장 모델: 기존 시스템(RDB/문서형/그래프)과의 선택/연동 전략 차이
- API 규격: 이미 정의된 내부 표준과 엔드포인트/페이로드 상충 여부
- 평가 지표: 다른 에이전트 평가체계와 지표 정의 충돌 여부
- 릴리즈/버전: 현행 배포 파이프라인과 버전 정책 불일치 여부

### 19. Step 1 온보딩 카드 사양 및 클릭 연결
- **카드 제목/아이콘/색상**: 온보딩(👤), `#3b82f6`
- **입력 필드(inputs)**: [학생 ID, 학년/반, 기존 데이터, MBTI]
- **출력 필드(outputs)**: [프로필 정보, 학습 이력, 선호도 설정, 성격 유형]
- **UI 특성**: 사용자 입력 요구(requiresUserInput=true), `userInputType='onboarding'`
- **세부 링크(detailLink)**: `https://mathking.kr/moodle/local/augmented_teacher/alt42/orchestration/onboarding_learningtype.php`
- **MBTI 선택 UI 버튼**: '하이페리아' 버튼 클릭 시 `https://mathking.kr/moodle/local/augmented_teacher/alt42/studenthome/wxsperta/wxsperta.php` 새 창 오픈
- **GPT 링크(G 버튼, customGPTLinks[1])**: `https://chatgpt.com/g/g-68c159847c708191ac0a0c6bfc67c5c3`

#### 19.1 클릭 액션 상세
- 진화점 G 버튼 클릭: `customGPTLinks[1]` 열림
- 제목 링크(밑줄) 클릭: `detailLink` 열림
- MBTI 드롭다운 옆 버튼(하이페리아) 클릭: `.../studenthome/wxsperta/wxsperta.php` 열림

#### 19.2 상호연결 맵(요약)
- Step 1 → 온보딩 상세 폼: `onboarding_learningtype.php`
- Step 1 → GPT 사전설정: `customGPTLinks[1]`
- Step 1 → WXSperta(학습자 홈): `wxsperta.php`

#### 19.3 온톨로지/용어집 매핑
- `학생 ID` → 온톨로지 `studentId` (xsd:integer) / 용어집 key `studentId`
- `학생 이름`(state 반영) → 온톨로지 `studentDisplayName|studentLegalName`
- `MBTI` → 온톨로지 `mbtiType` (LearningStyleProfile.mbti) / 용어집 `mbtiType`
- 기타 기본정보(학교/학년 등)는 후속 입력 화면(`onboarding_learningtype.php`)에서 수집되어 온톨로지 `Institution.schoolName`, `gradeLevel`, `curriculumStage` 등에 매핑됨

#### 19.4 데이터 흐름
- 초기 로딩 시 PHP→JS 데이터 병합: `phpData.studentId`, `phpData.studentName`가 `state.stepData[1].inputs`에 주입
- `state.selectedOptions.mbtiType` 변경 시 Step 1 IO 동기화: `inputs['MBTI']`, `outputs['성격 유형']` 업데이트
- 저장 이벤트: 주요 단계(5,10,15,21)에서 `saveOnboardingData()` 호출로 온보딩 데이터 서버 저장(참고: step1은 표시/연결 중심)

#### 19.5 관련 코드 레퍼런스
```1:36:h:\내 드라이브\하이페리얼 테크놀로지스\03-Resources\orchestrationk\assets\js\workflow_state.js
const steps = [
  { id:1, title:"온보딩", icon:"👤", color:"#3b82f6", requiresUserInput:true, userInputType:"onboarding", detailLink:"https://mathking.kr/moodle/local/augmented_teacher/alt42/orchestration/onboarding_learningtype.php" },
];
```
```695:743:h:\내 드라이브\하이페리얼 테크놀로지스\03-Resources\orchestrationk\assets\js\workflow_render.js
const customGPTLinks = { 1: 'https://chatgpt.com/g/g-68c159847c708191ac0a0c6bfc67c5c3', ... };
function copyToClipboardAndOpenGPT(stepId){ const gptLink = customGPTLinks[stepId]; if (gptLink) window.open(gptLink,'_blank'); }
```
```8:20:h:\내 드라이브\하이페리얼 테크놀로지스\03-Resources\orchestrationk\assets\js\workflow_events.js
function renderMbtiSelect(step){
  return `<button onclick="window.open('https://mathking.kr/moodle/local/augmented_teacher/alt42/studenthome/wxsperta/wxsperta.php','_blank')" class="chip">하이페리아</button>`;
}
```

#### 19.6 상충 가능성 체크(본 문서/용어집/온톨로지 대비)
- 학습스타일 입력 표기: 본문 일부에 '엽력' 오탈자 존재 가능 → 용어집의 `learning_style_form` 표기로 통일 권장
- Step 1에서 직접 저장 로직 없음: 저장은 5,10,15,21 단계에서 수행 → 온보딩 초반 저장 기대와 차이 가능(운영 가이드에 명시 필요)
- `학생 이름` 처리: 용어집은 display/legal 분리 제안, 현재 UI는 표시명만 주입 → 법적 이름 수집 경로 명시 필요
- GPT 링크 다수: 사내 표준 프롬프트/모델 활용 정책과 정합성 점검 필요



