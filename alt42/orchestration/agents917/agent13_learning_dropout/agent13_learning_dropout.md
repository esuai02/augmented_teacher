# Agent 13 Learning Dropout 지식파일

## 목적/스코프
- 최근 24시간(롤링 윈도우) 동안의 학습 이탈 조짐을 조기 탐지하고 예방 개입을 설계한다.
- 이탈이 빈번한 학생을 식별하고 원인을 추정하여 맞춤형 피드백을 제공한다.
- 쉬운 승리 구간을 제안하여 필요한 활동으로 빠르게 재진입하도록 가이드한다.

윈도우 정의: `T_now` 기준 `[T_now-86400, T_now]`. 당일 집계와 호환되며, 추세 분석은 별도 에이전트로 확장한다.

## 데이터 소스(읽기 기준)
- `mdl_abessi_messages`: `tlaststroke`, `timemodified` (노트/보드 최근 입력 및 수정)
- `mdl_abessi_today`: `ninactive`, `nlazy`, `activetime`, `checktime`, `status`, `type in ('오늘목표','검사요청')`
- `mdl_abessi_tracking`: `status`, `timecreated`, `duration`, `text`
- `mdl_abessi_indicators`: `npomodoro`, `kpomodoro`, `pmresult`, `nalt`
- `mdl_user`: `lastaccess`

## 핵심 지표 정의(24h 롤링)
- **이탈 이벤트 수(ninactive)**: 5분 쿨다운 정책으로 증가하는 경고 카운터. 24h 윈도우 내 증가분 또는 최신행 값 사용 정책을 명시한다.
- **지연 시청(👀)**: `timespent = (T_now - messages.timemodified)/60 ≥ 5` 인 이벤트 수.
- **무입력 지속시간(tlaststroke)**: `T_now - min(messages.tlaststroke, goal.timecreated, tracking.timecreated)`.
- **루틴 지연(nlazy)**: 20분당 1 블록으로 환산하여 표시(`round(nlazy/20)`).
- **포모도로 상태**: `npomodoro`, `kpomodoro`, `pmresult`(만족도). `npomodoro < 5` 이면 Alert 상태로 본다.

## 판단 알고리즘(요약 규칙)
- **이탈 판정**: `tlaststroke ≥ 300s` 그리고 `< 43200s` 구간을 이탈로 분류.
- **이탈 카운트 증가**: `if (goal.checktime < T_now-300 && goal.inspect==0) ninactive += 1; checktime = T_now; else activetime += 300; if (activetime > 3600) { activetime = 0; ninactive -= 1; }`
- **👀 표기**: `timespent ≥ 5`일 때 숫자 대신 👀 아이콘으로 표시(노트는 열려있으나 수정 없음).
- **상태 구간**: `<30s`, `<60s`, `<180s`, `<300s`, `5분~12시간(이탈)`로 테이블 분류.

## 위험 등급(24h)
- **Low**: `ninactive ≤ 1` 그리고 `npomodoro ≥ 5`.
- **Medium**: `ninactive ∈ [2,3]` 또는 `npomodoro ∈ [2,4]` 또는 `👀 ≥ 2회`.
- **High**: `ninactive ≥ 4` 또는 `npomodoro < 2` 또는 `tlaststroke ≥ 30분` 1회 이상.

## 휴리스틱(원인 추정)
- 반복적 **필기 지연(👀·tlaststroke 동시↑)** → 동기/행동활성 저하 가능성 높음.
- **npomodoro < 5** 반복 → 단기목표 루틴 미정착. 세션 길이 및 시작 의식 설계 필요.
- **로그인 후 목표 입력 지연/누락** → 시작 장벽 높음. 목표 작성 보조와 콜드스타트 루틴 권장.
- **데이터 모니터링 비활성** → 로그 결손 위험. 복구 절차 안내 필요.

## 개입 액션 플레이북
- **즉시 개입**: 짧은 리포커스 메시지 전송(링크 포함), 쉬운 승리 태스크 제시, 타임스캐폴딩 10~15분 프리셋.
- **루틴 보정**: 세션 길이 15→10분 시범, 휴식 알람 50→40분, 재진입 알림 리마인더.
- **에스컬레이션**: High 등급 2일 연속 시 보호자/담임 알림(운영정책에 따름).

## 코칭/리마인더 프롬프트 템플릿
- 리포커스: "지금부터 10분만 문제 풀이에만 집중해요. 끝나면 1분 휴식!"
- 콜드스타트: "오늘 목표를 한 줄로 적고, 10분 세션을 시작해볼까요?"
- 루틴 유도: "타이머 10분 시작 → 문제 1개 → 체크리스트 1줄 기록까지 완료!"

## 24h 롤링 집계 규칙(읽기 예시)
- 기준시각 `T_now`, 윈도우 `[T_now-86400, T_now]`.
- 예시(개념):
  - ninactive(증가분 집계형): 최신 `abessi_today`의 `ninactive`를 사용하거나, 24h 동안 증가한 이벤트 로그를 합산. 구현체 규칙에 맞춰 선택.
  - 👀 이벤트: `messages.timemodified ≥ T_now-86400` 중 `timespent ≥ 5` 사례 수 계산.
  - 포모도로: 최신 `indicators`의 `npomodoro`, `kpomodoro`, `pmresult`를 사용.

## 리포트 출력 스키마(요약)
- 입력: `userId`, `T_now`(epoch)
- 출력: `{ riskTier, evidence: { ninactive, eyeCount, tlaststrokeMin, npomodoro, kpomodoro, pmresult, nlazyBlocks },
  actions: [즉시 개입 1~2개], prompts: [2~3개] }`

## 경계/예외
- 신규/희소 데이터: 지표 수 미달 시 Medium 이하로 완충하고 코칭만 제시.
- 오프라인(`inspect in (2,3)`) 구간은 별도 섹션에 표기하며 ninactive 증가 로직은 운영 규칙 준수.

Last-Updated: 2025-10-18
