# 진도 리포트 ({{date}})

## 학습 현황

- **현재 진도**: {{current_progress|percent}}
- **평균 대비**: **{{progress_delta|percent}}** {% if progress_delta < -0.2 %}🔴{% elif progress_delta < -0.1 %}⚠️{% else %}ℹ️{% endif %}
- **최근 집중지수**: {{focus|round(2)}} / 1.0
- **학습 시간**: {{time_on_task_min}}분 (최근 {{lookback_days}}일)

## 분석

{% if progress_delta <= -0.2 %}
⚠️ **주의**: 평균보다 **{{(progress_delta|abs * 100)|round(0)|int}}%** 뒤처져 있습니다.
{% elif progress_delta <= -0.1 %}
ℹ️ 평균보다 약간 느립니다. 집중력을 높이면 따라잡을 수 있습니다.
{% else %}
✅ 진도가 회복되고 있습니다!
{% endif %}

{% if retry_count >= 3 %}
- 최근 **{{retry_count}}회** 재시도 → 난이도 조정이 필요할 수 있습니다.
{% endif %}

## 제안

{% if strength >= 0.8 -%}
**지금 바로 다음 활동을 시작하세요:**
{%- elif strength >= 0.6 -%}
**다음 활동을 우선 후보로 고려하세요:**
{%- else -%}
다음 활동을 참고해보세요:
{%- endif %}

- 📚 **{{unit_name}}** 파트 집중 학습
- ⏱️ 예상 소요: **{{minutes}}분**
- 🎯 목표: 평균 진도 도달

{% if chart_ref %}
## 그래프

![진도 그래프]({{chart_ref}})
{% endif %}

---
*생성 시각: {{timestamp}}*
*신뢰도: {{confidence|round(2)}}*
