🎯 Agent 13 — Learning Dropout Mission

**📌 데이터 참조 구조**

이 에이전트는 다음 마스터 소스의 데이터를 참조합니다:
- **감정/정서 데이터**: `ref(agent05.*)` - Agent 05 (학습감정 분석) 참조
- **루틴/리듬 데이터**: `ref(agent18.*)` - Agent 18 (시그너처 루틴 탐색) 참조

(이탈 감지·복귀 신호·지속 루틴 진단 전용 100항목)
🧠 1️⃣ 이탈 이벤트 및 경고 데이터 (10)

이탈 이벤트 ID

이탈 발생 시각

이탈 전 학습모드(개념/문제/복습 등)

이탈 전 감정 상태(안정/권태/불안 등) → `ref(agent05.emotion_state_before_dropout)`

이탈 전 집중도(0~1.0)

이탈 후 무입력 지속시간(분)

ninactive 누적 횟수(24h 기준)

동일 시간대 이탈 재발 빈도(요일 패턴)

이탈 이벤트 간 평균 간격(분 단위)

이탈 시점 감정기울기(Δemotion) → `ref(agent05.emotion_slope_at_dropout)`

⏱️ 2️⃣ 시간 기반 이탈 패턴 (10)

세션 시작 후 첫 이탈 시점(분 단위)

세션 종료 전 마지막 이탈 시점

시간대별 이탈 확률(Heatmap)

포모도르 단위당 평균 이탈율

주중·주말 이탈율 차이

집중 시작 후 이탈까지 지속시간 평균

연속 이탈 이벤트 수(30분 롤링 윈도우)

이탈 전 활동단계(개념→문제→이탈 등)

이탈 후 복귀까지 평균 지연시간

시간대별 ‘복귀 성공률’(이탈 후 재집중 비율)

📊 3️⃣ 학습 지속력 및 리듬 지표 (10)

세션 평균 지속시간(분)

중단 없는 학습 세션 비율(%)

하루 평균 이탈 이벤트 수

주간 지속성 지수(ML 추정값)

세션별 몰입곡선 길이(유효시간 구간 길이)

학습시간 대비 실제 집중시간 비율

루틴 붕괴 빈도(일 단위) → `ref(agent18.routine_collapse_frequency)`

루틴 붕괴 후 복귀 성공률(%) → `ref(agent18.routine_recovery_success_rate)`

루틴 복귀 소요시간 평균(분)

루틴 붕괴 재발주기(일 단위)

⚡ 4️⃣ 복귀 신호(Re-entry Cues) 데이터 (10)

복귀 이벤트 발생 시각

복귀 후 첫 입력까지 시간

복귀 직후 감정 안정도 → `ref(agent05.emotion_stability_after_reentry)`

복귀 직후 학습 지속시간

복귀 직후 문제 풀이 정확도

복귀 직후 포모도르 유지시간

복귀 직후 필기 반응속도

복귀 후 이탈 재발 간격

복귀 이벤트 성공률(집중 지속 10분 이상 비율)

복귀 신호 유형(자발/시스템유도/교사개입)

❤️ 5️⃣ 정서적 피로 및 권태 누적 (10)

감정 에너지 감소율(최근 10분 대비 Δ%) → `ref(agent05.emotion_energy_decrease_rate)`

감정 피로누적 점수(최근 1h 이동평균)

감정 불안정 구간 지속시간

권태 신호(지루함/하품/무표정) 감지빈도

감정 안정도 변동계수(표준편차) → `ref(agent05.emotion_stability_variance)`

피로도 대비 이탈 확률(상관계수)

감정 급락→이탈 시간지연(분)

감정 회복 실패 후 연속이탈 횟수 → `ref(agent05.consecutive_dropout_after_recovery_failure)`

감정 회복 성공 후 집중 지속시간

권태 패턴 지속일수(최근 7일)

📚 6️⃣ 학습행동 저하 로그 (10)

입력 간 평균 간격 변화율(최근 10분 대비)

클릭/필기 빈도 급감 시점

페이지 전환 멈춤률

문제풀이 진행률 하락폭

활동 간 전이 실패율(개념→문제 미전환 비율)

학습활동 Idle 비율(5분 이상 무활동 비율)

학습탭 외부 이동 감지횟수

TTS 청취 중 이탈율

시선 이탈 누적시간(초 단위)

학습 포기 행동 감지(창 닫기/탭 전환 등)

🧩 7️⃣ 루틴 일관성 및 안정성 (10)

루틴 시작 시각 변동폭(분 단위 표준편차)

루틴 유지시간 편차

루틴 유지 실패일수(주간)

루틴 재개 성공률

루틴 재개까지 평균 공백시간

루틴 패턴 안정도 지수(0~1) → `ref(agent18.routine_pattern_stability_index)`

루틴 붕괴 후 감정 회복시간 → `ref(agent05.emotion_recovery_time_after_routine_collapse)`

루틴 유지 중 감정 안정도 평균 → `ref(agent05.average_emotion_stability_during_routine)`

루틴 시간대별 성공확률

루틴 피로누적 전환점(학습→이탈 지점)

🔍 8️⃣ 인지 부하 및 집중 저하 (10)

문제 난이도 대비 체류시간 비율

인지부하 급상승 시점(평균 대비 2σ 초과)

시선 집중 분산도(시선좌표 분산값)

입력·시선 불일치율

연속 멍때림 구간(>20초) 빈도

사고 전환 지연시간(문제 읽기→풀이 시작)

집중도 급락 이벤트 수(시간당)

집중 회복 시도 성공률

집중도 변동률(10분 이동평균)

인지피로 회복속도(집중도 회복까지 평균분)

💬 9️⃣ 개입 반응 및 효과 데이터 (10)

개입 메시지 수신 시점

개입 후 복귀 성공 여부

개입 후 학습 지속시간(분)

개입 후 감정 변화폭(Δemotion) → `ref(agent05.emotion_change_after_intervention)`

개입 후 포모도르 유지시간 변화율

개입 후 정답률 변화율

개입 후 이탈 재발 간격

개입 후 몰입도 회복률

개입 반복 시 내성도(효과 감소율)

개입 후 피드백 반응시간(초 단위)

🧭 🔟 장기 패턴 및 예측 메타데이터 (10)

일자별 이탈 트렌드(7일 이동평균)

주간 이탈률 변화율(%)

월간 지속력 점수(ML 기반 추정)

이탈 위험도 예측값(1h horizon)

복귀 성공률 예측값(다변량 회귀결과)

루틴 유지 확률(다음 세션 예측)

감정-이탈 상관계수(rolling correlation) → `ref(agent05.emotion_dropout_correlation)`

피로-이탈 상관계수

개입 효과 누적지수(전일 대비 변화%)

복귀 루프 완결률(이탈→개입→복귀→유지 완료 비율)